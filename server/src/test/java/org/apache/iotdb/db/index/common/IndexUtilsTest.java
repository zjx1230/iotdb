package org.apache.iotdb.db.index.common;

import static org.junit.Assert.*;

import org.apache.iotdb.db.exception.metadata.IllegalPathException;
import org.apache.iotdb.db.metadata.PartialPath;
import org.junit.Assert;
import org.junit.Test;

public class IndexUtilsTest {

  @Test
  public void match() throws IllegalPathException {
//    Assert.assertTrue(IndexUtils.match(new PartialPath("root.v.*.s1"), new PartialPath("root.v.d1.s1")));
//    Assert.assertFalse(IndexUtils.match(new PartialPath("root.v.*.*.s1"), new PartialPath("root.v.d1.s1")));
//    Assert.assertFalse(IndexUtils.match(new PartialPath("root.v.d1.*.*"), new PartialPath("root.v.d1.s1")));
//    System.out.println("a.1".matches("a.*"));
  }
}